apiVersion: v1
kind: Service
metadata:
  name: retell-webhook-service
  # namespace: retell-webhooks
  labels:
    app: retell-webhook-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: retell-webhook-service
